<div class="row">
  <div class="col-md-10">
    <div class="my-4" id="loginContent">

        <h2>New Album</h2>
        <%= form_with(model: @image, local: true) do |f| %>
          
          <div class="form-group">
            <%= f.label :imgURL , "Image cover"%>
            <br />
            <%= f.file_field :imgURL, autocomplete: "off", class: "btn btn-primary" %>
          </div>
          <%= f.hidden_field :album_id, :value => params[:id] %>
          <div class="actions">
            <%= f.submit "Post", class: "btn btn-primary" %>
          </div>
        <% end %>
    </div>
  </div>
</div>
