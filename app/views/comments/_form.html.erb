<%= form_with(model: comment, local: true) do |f| %>
  <div class="form-group">
    <%= f.text_area :content, autofocus: false, class: "form-control", rows: "3" %>
  </div>
  <div class="actions">
    <%= f.submit "Post", class: "btn btn-primary" %>
  </div>
<% end %>
