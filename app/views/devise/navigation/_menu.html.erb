<nav id="topNav" class="navbar navbar-expand-md">
    <div class="container">
        <a class="navbar-brand mr-3" href="/">WhiteAlbums</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#collapsingNavbar">
            ☰
        </button>
        <div class="collapse navbar-collapse" id="collapsingNavbar">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link page-scroll pr-3" href="/" HomeHight breacum="home">Home
                        <span class="sr-only">(current)</span>
                    </a>
                </li>
                <li class="nav-item">
                    <a class="nav-link page-scroll pr-3" href="/albums" AlbumsHight breacum="albums">Albums</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link page-scroll pr-3" href="/abouts" AboutHight breacum="about">About</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link page-scroll pr-3" href="/blogs" BlogsHight breacum="blogs">Blogs</a>
                </li>
            </ul>
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <% if user_signed_in?%>
                    <div class="dropdown">
                      <a class="nav-link page-scroll" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <div class="classImage">
                          <%= image_tag(current_user.picture.url(:thumb)) %>
                        </div>
                        <%= current_user.name %>
                      </a>
                      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <a class="dropdown-item" href="#">Profile</a>
                        <%= link_to new_blog_path, class: "dropdown-item" do %>
                          Post blog
                        <% end %>
                        <%= link_to destroy_user_session_path, method: "delete", class: "dropdown-item" do %>
                          Logout
                        <% end %>
                      </div>
                    </div>
                    <% else %>
                    <div class="dropdown">
                      <a class="nav-link page-scroll" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Your Account
                      </a>
                      <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                        <%= link_to new_user_session_path, class: "dropdown-item" do %>
                          Login
                        <% end %>
                        <%= link_to new_user_registration_path, class: "dropdown-item" do %>
                          Register
                        <% end %>
                      </div>
                    </div>
                    <% end %>
                </li>
            </ul>
        </div>
    </div>
</nav>
